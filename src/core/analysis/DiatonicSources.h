#pragma once
#include "core/model/ImprovisationContracts.h"
namespace smartimproviser::harmony
{
// Adds at most one scale strategy after the explicit-chord foundation.
void addDiatonicSource(ImprovisationResult& result);
}
