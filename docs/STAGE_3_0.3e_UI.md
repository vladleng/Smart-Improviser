# Stage 3 / 0.3e-ui — структурная разгрузка интерфейса

`0.3e-ui` — отдельный UI-checkpoint поверх музыкальной логики 0.3e и русификации fix2. Цель — сделать live-тесты следующих подэтапов удобными, не превращая текущий диагностический editor в финальный продуктовый дизайн.

## Что меняется

- Верхняя карточка **«Текущий контекст»** остаётся видимой всегда и показывает:
  - текущий и следующий аккорд;
  - тональность, гармоническую функцию и локальный центр;
  - распознанный гармонический оборот и позицию текущего аккорда в нём;
  - краткую логику мышления для импровизации: доступный лад / m6-thinking и подтверждённое голосоведение в следующий аккорд.
- Названия функций в UI компактные: **Тоника / Субдоминанта / Доминанта / SubV**. Слово «функция» внутри самого значения не дублируется.
- В строке оборота роль не повторяется второй раз: при наличии позиции отображается компактно, например **`Оборот: Мажорный ii-V-I • 1 / 3`**.
- Большой прежний экран «всё сразу» заменён на четыре переключаемых/сворачиваемых раздела:
  1. **Материал** — открыт по умолчанию; harmonic concepts и практические идеи для импровизации.
  2. **Источники / ноты** — гаммы, m6-thinking, chord anchors, guide tones, characteristic tones, targets и resolution moves.
  3. **Гармонический анализ** — global/local function, pattern, interpretation, confidence и candidates.
  4. **ARA / диагностика** — binding, host access, transport, PPQ, события и revisions.
- В каждый момент открыт один раздел; остальные отображаются как закрытые спойлеры. Это сохраняет компактную высоту окна и не требует внешнего скролла всей формы.
- Длинный контент каждого раздела прокручивается внутри собственной области.
- Музыкальная логика Core, Harmonic Engine, Improvisation Engine, rule IDs и результаты анализа не меняются.

## Логика строки «Мышление»

Это краткий summary уже существующих данных Stage 3, а не новый музыкальный анализ:

- для диатонической стратегии показывается название лада в привычной музыкальной форме, например **`D Dorian`**;
- для special-source стратегии с thinking structure показывается именно её аккордовая форма, например **`Dm6`** или **`Abm6`**;
- поэтому типичный summary для Dm7 выглядит как **`Мышление: D Dorian • Dm6`**;
- если Stage 2 подтвердил разрешение, после материала добавляются реальные голосоведческие движения и целевой аккорд;
- если scalar source недоступен, остаётся fallback **«звуки аккорда + направляющие тоны»**.

Таким образом верхняя карточка отвечает на три практических вопроса: **что сейчас звучит, какой это оборот и как обыгрывать этот аккорд в данном контексте**.

## Граница текущей UI-работы

На этом `0.3e-ui` считаем структурно сформированным. Дальнейшую полировку интерфейса временно останавливаем и не расширяем layout без музыкальной необходимости.

Пока сознательно не делаем:

- финальный визуальный стиль;
- Tension UI / T1–T3 controls;
- production-level карточки, иконки и анимации;
- пользовательские настройки layout;
- нотный/табулатурный editor.

К полноценному product UI логичнее вернуться после появления Tension Engine.

## Live-test

Artifact: `Smart-Improviser-0.3e-ui-Windows`  
Build label: `0.3e-ui`

Проверить:

- [ ] окно открывается без обрезания и заметно компактнее старого диагностического layout;
- [ ] сверху всегда видны текущий аккорд и тональность;
- [ ] в верхней карточке отображается функция коротким названием: **Тоника / Субдоминанта / Доминанта / SubV**;
- [ ] строка оборота компактная и не дублирует роль: например `Мажорный ii-V-I • 1 / 3`;
- [ ] для Dm7 в C major строка **«Мышление»** показывает **`D Dorian • Dm6`**, когда оба варианта доступны;
- [ ] для контекста без scalar source остаётся fallback `звуки аккорда + направляющие тоны`;
- [ ] подтверждённые resolution moves при наличии остаются в summary;
- [ ] по умолчанию открыт раздел **Материал**;
- [ ] четыре кнопки переключают разделы и корректно меняют `▶ / ▼`;
- [ ] **Материал** содержит harmonic concepts, но не техническую диагностику;
- [ ] **Источники / ноты** содержит гаммы, m6, опорные/направляющие/характерные ноты, targets и resolution;
- [ ] **Гармонический анализ** содержит global/local анализ, трактовку, confidence и candidates;
- [ ] **ARA / диагностика** содержит прежние технические данные Stage 1;
- [ ] скролл внутри активного раздела работает;
- [ ] неизменный контекст не сбрасывает скролл на каждом timer tick;
- [ ] смена аккорда/seek обновляет содержимое и summary;
- [ ] UTF-8 и русификация fix2 не регрессировали;
- [ ] Db7/SubV spelling из 0.3d fix2 сохранён;
- [ ] все 11 C++ regression targets остаются зелёными.

После принятия `0.3e-ui` возвращаемся к музыкальному тесту/закрытию 0.3e и затем к 0.3f.
