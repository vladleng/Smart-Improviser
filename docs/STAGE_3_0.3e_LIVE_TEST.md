# Stage 3 / 0.3e — Harmonic concepts

База: принятый 0.3d fix2, Windows Build #246 success, PR #26 merged.
0.3e реализован; первый вариант русификации `0.3e fix1` не принят live-test: часть прямых JUCE-строк отображалась с mojibake, а часть обычных английских слов осталась непереведённой. Текущая проверяемая версия — **0.3e fix2**. Stable остаётся 0.3.

## 0.3e fix2 — полная русификация и UTF-8

Fix2 не меняет Harmonic Engine / Improvisation Engine и музыкальные результаты. Изменяется только presentation layer диагностического editor.

Что исправлено:

- все русские литералы, которые непосредственно попадают в JUCE UI, создаются через `juce::String::fromUTF8`; это устраняет зависимость от неявной трактовки `const char*` и проблему «кракозябр» из fix1;
- переведены подписи и статусы этапов 1/2/3, названия блоков, трактовок, оборотов, функций, confidence/status и пояснения;
- переведены обычные английские слова внутри harmonic concepts: `Phrase` → «фраза», `guide tones` → «направляющие тоны», `target/current/next chord`, `preparation`, `passing`, `approach/enclosure` и т. п.;
- названия ладов/источников в presentation layer отображаются по-русски (`Dorian` → «дорийский», `Mixolydian` → «миксолидийский», `melodic minor` → «мелодический минор», `Lydian dominant` → «лидийский доминантовый»);
- порядок замен исправлен: сначала переводятся целые инструкции (`Prepare over the current chord...`), и только потом короткие фрагменты. Это исключает смешанные строки вида `Prepare поверх ...`;
- `guide tones` в отдельном диагностическом блоке теперь подписаны **НАПРАВЛЯЮЩИЕ ТОНЫ (3 / 7)**.

Без перевода остаются только обозначения, для которых перевод ухудшает однозначность или не нужен: названия нот и аккордов (`Db7`, `Cmaj7`, `F#`, `Bb`), римские гармонические схемы (`ii-V-I`, `V-I`), `ARA`, `PPQ`, `BPM`, `MIDI`, `SubV`, а также транспортные `PLAY/STOP`.

## Объём и граница с фразой

Checkpoint добавляет текстовые гармонические идеи поверх уже проверенных Strategy. Хроматический подход/окружение здесь — символические схемы интервалов относительно цели, а не короткие сгенерированные фразы. Нет ритма, длительностей, октав, аппликатуры, MIDI, воспроизведения или записи пользовательских нот. Генерация и редактирование фраз остаются последующими этапами.

| Идея | Материал / поведение |
|---|---|
| Игра по звукам аккорда | Реальные звуки текущего аккорда с написанием и ступенями; не добавляет отсутствующие 3/7 |
| Ведение по направляющим тонам | Только имеющиеся 3/7; подтверждённые Stage 2 движения отделены от необязательных мелодических связок |
| Диатонические краски | 9/11/13 из уже доступного диатонического источника; natural 11 против major 3 и b13 против natural 5 помечаются как проходящие |
| Мышление через m6 | Четыре звука уже доступной m6-структуры; интервалы показываются относительно реального аккорда, полный источник сохраняется |
| Хроматический подход | Ниже цели на полутон → цель; задана реальная целевая нота |
| Хроматическое окружение | Выше на полутон → ниже на полутон → цель; начальная схема, не полный каталог enclosure |

Подход/окружение — продуктовые шаблоны, не транскрипции фраз Бойко. m6 — продуктовая инструкция, привязанная к принятому каталогу источников раздела 2. Новые заменяющие гаммы, Fm6 над G7 и outside-системы не добавляются.

## Контекст и роли

- Concepts хранят rule ID/version, sourceRuleId, interpretation/evidence и actualChord. Списки Strategy и их приоритеты не меняются.
- MaterialNote в concept.material относится к actualChord. Target относится к отдельному targetChord; targetScope явно currentChord/nextChord.
- Цель подхода: сначала присутствующая терция следующего аккорда, затем другая направляющая нота, затем присутствующий корень. Если следующего аккорда нет, тот же выбор делается внутри текущего.
- Подготовительные ступени оцениваются относительно текущего реально звучащего аккорда. Принадлежность аккорду не является оценкой устойчивости или Tension Level.
- При цели следующего аккорда инструкция предлагает подготовку на текущей гармонии и попадание в цель при смене аккорда. Точного момента начала/длины фразы в данных нет.
- Primary unresolved сохраняется. Идеи по explicit chord/next notes доступны без выбора тонального центра; диатонические/m6 идеи появляются только вместе с соответствующими уже разрешёнными источниками.
- Fix2 предыдущего checkpoint сохраняется: host C#7 перед C major/minor при confirmed SubV даёт Db7/Abm6 в новых идеях; raw Stage 1/2 может оставаться C#7.
- Функция, глобальный/локальный центр, confirmed resolution и пользовательские ноты не меняются.
- По-прежнему нет автоматической классификации T1–T3. Сравнение global/local alternatives и ranking — 0.3f.

## Проверка в Fender Studio Pro

Artifact `Smart-Improviser-0.3e-fix2-Windows`, установленная папка `Smart Improviser.vst3`, заголовок **0.3e fix2**. Блок **ГАРМОНИЧЕСКИЕ ИДЕИ** стоит перед **ИСТОЧНИКИ ГАММ** в прокручиваемой правой панели.

| Сценарий | Ожидается |
|---|---|
| C major / Dm7 → G7 → Cmaj7, на G7 | Игра по звукам аккорда: G B D F; направляющие тоны B/F, подтверждённые B→C и F→E |
| Тот же G7 | Диатонические краски: A (9), C (11) [проходящая], E (13) |
| Тот же G7 | Мыслить Dm6 поверх G7: D (5), F (b7), A (9), B (3); Мыслить Abm6 поверх G7: Ab (b9), B (3), Eb (b13), F (b7) |
| Тот же G7 | Хроматический подход/окружение: цель E (3) в Cmaj7 [следующий аккорд]; схемы -1→0 и +1→-1→0 |
| G7 → Cm | Цель становится Eb (b3) в Cm; подтверждённое F→Eb сохранено |
| Cmaj7 → Dm7 → G7, на Dm7 | Связки направляющих тонов к G7 остаются необязательными, а не подтверждёнными |
| Db7 → Cmaj7 / Cm | Идея «Мыслить Abm6 поверх Db7»; функциональное написание 0.3d fix2 сохранено |
| G7sus4 → Cmaj7 | В направляющих тонах только F (b7), отсутствующая B не добавляется |
| C major / C трезвучие, без next | Направляющая нота только E; подход направлен к E текущего аккорда; септима не добавляется |
| C5 без next | Нет идеи с направляющими тонами, подход направлен к C текущего аккорда |
| C major / Dm7b5 → G7 при unresolved primary | Идеи по явно заданным нотам допустимы; неподтверждённые scale/m6 идеи не появляются |
| C# major / C#maj7 | Новые аккордовые опоры пишутся C# E# G# B# |

Последние диагностические списки старых checkpoints могут сохранять pitch-class запись; новые harmonic ideas используют ступенное написание.

- [ ] В левой и правой панелях нет mojibake/кракозябр: вся кириллица читается нормально.
- [ ] Нет смешанных русско-английских обычных фраз (`Prepare поверх`, `Phrase`, `next chord` и т. п.).
- [ ] Ноты/аккорды, `ARA`, `PPQ`, `BPM`, `MIDI`, `SubV`, `PLAY/STOP` не искажены.
- [ ] Все основные блоки интерфейса помещаются/прокручиваются; длинные русские строки не скрывают музыкально важные значения.
- [ ] PLAY/STOP/seek, изменения аккорда и цели major/minor обновляют идеи вместе с источниками.
- [ ] После удаления валидного контекста старые идеи исчезают; reopen восстанавливает результат.
- [ ] Регрессии принятого 0.3d fix2 не обнаружены.

## Автоматическая проверка

11 C++ test targets из 0.3e остаются без изменения: fix2 затрагивает presentation layer и build label/artifact. Windows CI должен подтвердить компиляцию UTF-8 строк и отсутствие regression существующих тестов.

После принятия **0.3e fix2** закрываем 0.3e и переходим к 0.3f.
